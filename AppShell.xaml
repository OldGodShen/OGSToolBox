<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="OGSToolBox.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:OGSToolBox"
    xmlns:v="clr-namespace:OGSToolBox.Views"
    Shell.FlyoutBehavior="Disabled">

    <FlyoutItem>
        
    <ShellContent
        Title="金数据"
        ContentTemplate="{DataTemplate v:Jinshuju}"
        Route="Jinshuju" />
    
    <ShellContent
        Title="选课"
        ContentTemplate="{DataTemplate v:MainPage}"
        Route="MainPage" />

    </FlyoutItem>

</Shell>
