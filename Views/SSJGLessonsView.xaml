<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OGSToolBox.Views.SSJGLessonsView"
             Title="上师嘉高选科列表">
    <VerticalStackLayout>
        <Label Text="{Binding token}" FontSize="10"></Label>
        <Button Text="获取" Command="{Binding GetList}" FontSize="10"></Button>
        <Label Text="{Binding List}" FontSize="10"></Label>
        
    </VerticalStackLayout>
</ContentPage>